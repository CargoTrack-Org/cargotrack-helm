# AI Service — Dev Environment Overrides
global:
  environment: dev
  namespace: cargotrack-dev
image:
  registry: "692828329130.dkr.ecr.us-east-1.amazonaws.com"
  tag: "68ef1df0bc80f5f0b9afb676fb1adabf2dabd512" # CI/CD overwrites on every push
  pullPolicy: Always
serviceAccount:
  roleArn: "arn:aws:iam::692828329130:role/cargotrack-irsa-ai-service"
env:
  NODE_ENV: production
  MOCK_AGENT: "false"
  TEXTRACT_ENABLED: "true"
  LLM_PROVIDER: "bedrock"
  BEDROCK_MODEL_ID: "amazon.nova-lite-v1:0"
hpa:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  cpuTargetPercent: 70
  scaleInStabilizationSeconds: 600
